{
  "name": "cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:laptop": "npx cypress run --browser chrome --headed --config-file cypresslaptop.env.js",
    "test:mobile": "npx cypress run --browser chrome --headed --config-file cypressmobile.env.js",
    "testHeaded:chrome": "npx cypress run --headed --browser chrome",
    "testHeaded:firefox": "npx cypress run --headed --browser firefox",
    "testHeadless:chrome":"npx cypress run --headless --browser chrome",
    "testHeadless:firefox":"npx cypress run --headless --browser firefox"


  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^13.6.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "cypress-file-upload": "^5.0.8"
  },
  "browsers": {
    "chrome": {
      "name": "Chrome",
      "family": "chromium",
      "channel": "chrome"
    },
    "firefox": {
      "name": "Firefox",
      "family": "firefox",
      "channel": "mozilla"
    }
  }
}
